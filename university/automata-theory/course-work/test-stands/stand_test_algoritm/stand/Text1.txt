


по reset обнуляем counter
формируем в течении одного такта сигнал начала операции sno для ОУ
ожидаем сигнал конца операции sko
сравниваем полученный с ОУ результат с правильным в модуле test
если они совпадают, то есть enable='1' то переходим к п.1
если нет, то останов

подаем разрешение СЕ синхросигнала для модуля counter
операнды формируются модулем counter

